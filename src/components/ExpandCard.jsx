import React from "react";
import { styles } from "../styles";
import { SectionWrapper } from "../hoc";

const ExpandCard = () => {
  return <div>ExpandCard</div>;
};

export default SectionWrapper(ExpandCard, "");
